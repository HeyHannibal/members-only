extends layout

block content
  a(href='/comment') Leave a Comment
  if(messages)
    each message in messages
      div.message
      h2= message.title
      a(href=(`/`)) Go Back
      p= message.text
